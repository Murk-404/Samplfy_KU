declare type OptionalConfig = {
    port: number;
    scopes: string;
    fileName: string;
    outDir: string;
    noEmit: boolean;
};
declare type RequiredConfig = {
    clientId: string;
    clientSecret: string;
};
export declare type Options = RequiredConfig & Partial<OptionalConfig>;
export declare type DefaultConfig = RequiredConfig & OptionalConfig;
export declare type SpotifyTokenResponse = {
    access_token: string;
    token_type: 'Bearer';
    expires_in: number;
    refresh_token: string;
    scope: string;
    date_obtained: string;
};
export {};
