export declare type WriteJSONOptions = {
    path: string;
    fileName: string;
    data: unknown;
    compact?: boolean;
};
export declare const writeJSON: ({ path, fileName, data, compact, }: WriteJSONOptions) => Promise<string>;
