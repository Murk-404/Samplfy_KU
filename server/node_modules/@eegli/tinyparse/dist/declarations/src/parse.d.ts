import { InternalOptions, SimpleRecord } from './types';
declare type ParseObjLiteral<T> = {
    defaultValues: T;
    input: Partial<T>;
    options?: InternalOptions;
};
export declare function parseObjectLiteral<T extends SimpleRecord>({ defaultValues, input, options, }: ParseObjLiteral<T>): Promise<T>;
export {};
