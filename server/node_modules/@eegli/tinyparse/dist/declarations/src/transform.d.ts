import { InternalOptions, ParsingOptions, SimpleRecord } from './types';
export declare function transformOptions(parsingOptions?: ParsingOptions): InternalOptions;
declare type TransFormArgV = {
    argv: string[];
    options?: InternalOptions;
    filePathFlag?: `--${string}`;
};
export declare function transformArgv<T extends SimpleRecord>({ argv, options, filePathFlag, }: TransFormArgV): Partial<T>;
export {};
