import { FilePathArg, InternalOptions, SimpleRecord } from './types';
declare type DisplayHelp = {
    defaultValues: SimpleRecord;
    options?: InternalOptions;
    filePathArg?: FilePathArg;
    title?: string;
};
export declare const displayHelp: ({ defaultValues, options, title, filePathArg, }: DisplayHelp) => string;
export {};
