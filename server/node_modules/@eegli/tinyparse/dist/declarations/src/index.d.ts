import { ParsingOptions, SimpleRecord } from './types';
export { ValidationError } from './error';
/**
 * Parser factory function. Returns a parser that is bound to the
 * given default values and options per key.
 *
 * @export
 */
export declare function createParser<T extends SimpleRecord>(defaultValues: T, parsingOptions?: ParsingOptions<T>): {
    help: (title?: string) => string;
    parse: (input?: Partial<T> | string[]) => Promise<T>;
};
